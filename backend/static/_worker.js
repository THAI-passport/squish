// Cloudflare Pages advanced-mode entry point. The implementation also serves
// static assets through env.ASSETS, so the existing site keeps working.
export { default } from './squish-email-worker.js';
