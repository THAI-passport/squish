fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# PDF engine: page surgery, rendering, redaction, overlays.
# NOTE: PyMuPDF is AGPL-3.0. Fine for internal or open-source use; a commercial
# licence from Artifex is required to ship this inside a closed-source product.
PyMuPDF>=1.24

# Conversions
pdf2docx>=0.5.8
openpyxl>=3.1
python-pptx>=0.6.23

# Markdown -> PDF. WeasyPrint is a real CSS print engine (needs Pango system
# libs, installed in the Dockerfile). Pygments powers fenced-code highlighting.
Markdown>=3.5
weasyprint>=60
Pygments>=2.17

# Security
pyhanko>=0.21.0
